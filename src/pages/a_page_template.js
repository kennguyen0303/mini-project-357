import React, { Component } from 'react'

/**
 * 1. change "a_page" to the title of the section
 * 2. Change export "a_page" to the name created above
 * 3. keep the className the same, it is the name of the class in App.css 
 * 4. Import the component into App.js in this way: <name_of_the_export_default/>
 * 5. Developing this file and you should see the changes in the homepage
 */
const a_page=()=> {
        return (
            <div className="a_page" id="">
           
          </div>
        )
}

export default a_page
